# Capio Profiler tool

A simple python tool to inspect and generate basic statistics starting from an execution
trace of CAPIO.

Install:
```bash
    pip install git+https://github.com/High-Performance-IO/capio-profiler.git
```

Usage:
```bash
    python3 -m CapioProfiler </path/to/your/trace.log>
```